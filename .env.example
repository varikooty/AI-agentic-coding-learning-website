# Talks to a local Ollama server — no key needed. Install from https://ollama.com,
# then `ollama pull llama3.1:8b` (or your model of choice) before running the app.
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b
